clear;

# Chargement de la configuration
source ./settings.conf

# Protection
exit;

########################
# NAS Vers MyArchives1 #
########################

#rsync $OPTION "$DOCKER_SOURCE"     "$DOCKER_DESTINATION";
#rsync $OPTION "$DOWNLOAD_SOURCE"   "$DOWNLOAD_DESTINATION";
#rsync $OPTION "$HOME_SOURCE"       "$HOME_DESTINATION";
#rsync $OPTION "$MUSIC_SOURCE"      "$MUSIC_DESTINATION";
#rsync $OPTION "$SAUVEGARDE_SOURCE" "$SAUVEGARDE_DESTINATION";
#rsync $OPTION "$WEB_SOURCE"        "$WEB_DESTINATION";
#rsync $OPTION "$WINDOWS_SOURCE"    "$WINDOWS_DESTINATION";
